<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Blood Stock</title>
    <style>
        body {
            font-family: 'Open Sans', sans-serif;
            background-color: #F9F9F9;
            color: #333333;
            margin: 0;
            padding: 20px;
            text-align: center;
        }

        h1 {
            color: #C72C41;
            margin-bottom: 20px;
        }

        table {
            width: 80%;
            margin: auto;
            border-collapse: collapse;
            background-color: #ffffff;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        th, td {
            padding: 12px;
            border: 1px solid #ccc;
            text-align: left;
        }

        th {
            background-color: #C72C41;
            color: white;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        form {
            display: inline;
        }

        input[type="number"] {
            padding: 5px;
            margin-right: 10px;
            width: 120px; /* Set a fixed width for input */
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        button {
            background-color: #C72C41;
            color: #FFFFFF;
            border: none;
            padding: 5px 10px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #A82333; /* Darker shade on hover */
        }

        a {
            display: inline-block;
            margin-top: 20px;
            text-decoration: none;
            color: #007bff;
            transition: color 0.3s;
        }

        a:hover {
            color: #0056b3; /* Darker shade on hover */
        }
    </style>
</head>
<body>
    <h1>Manage Blood Stock</h1>
    
    <table>
        <thead>
            <tr>
                <th>Blood Type</th>
                <th>Quantity</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% if (stocks.length > 0) { %>
                <% stocks.forEach(stock => { %>
                    <tr>
                        <td><%= stock.bloodType %></td>
                        <td><%= stock.quantity %></td>
                        <td>
                            <form action="/volunteer/update-stock/<%= stock._id %>" method="POST">
                                <input type="number" name="quantity" min="0" required placeholder="New Quantity">
                                <button type="submit">Update</button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            <% } else { %>
                <tr>
                    <td colspan="3">No blood stock available.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
    
    <a href="/volunteer/dashboard">Back to Dashboard</a>
</body>
</html>
