<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Appointments</title>
    <style>
        body {
            font-family: 'Open Sans', sans-serif;
            background-color: #F9F9F9;
            color: #333333;
            margin: 0;
            padding: 20px;
            text-align: center;
        }

        h1 {
            color: #C72C41;
            margin-bottom: 20px;
        }

        ul {
            list-style-type: none; /* Remove default list styling */
            padding: 0;
            margin: 0;
        }

        li {
            background-color: #ffffff;
            border: 1px solid #ccc;
            border-radius: 8px;
            padding: 15px;
            margin: 10px 0;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s;
        }

        li:hover {
            transform: scale(1.02); /* Slightly increase size on hover */
        }

        p {
            margin: 5px 0; /* Spacing between paragraphs */
        }

        button {
            background-color: #C72C41;
            color: #FFFFFF;
            border: none;
            padding: 10px 15px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
            margin-top: 10px; /* Spacing above button */
        }

        button:hover {
            background-color: #A82333; /* Darker shade on hover */
        }

        .no-appointments {
            font-size: 18px;
            color: #666;
            margin-top: 20px;
        }

        a {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 15px;
            background-color: #C72C41;
            color: #FFFFFF;
            border-radius: 5px;
            text-decoration: none;
            transition: background-color 0.3s;
        }

        a:hover {
            background-color: #A82333; /* Darker shade on hover */
        }
    </style>
</head>
<body>
    <h1>Your Appointed Blood Camps</h1>

    <% if (appointedCamps.length > 0) { %>
        <ul>
            <% appointedCamps.forEach(appointment => { %>
                <li>
                    <h3><%= appointment.campId.name %></h3>
                    <p>Location: <%= appointment.campId.location %></p>
                    <p>Date: <%= new Date(appointment.campId.date).toLocaleDateString() %></p>
                    <p>Description: <%= appointment.campId.description || 'No description available' %></p>
                    <!-- Form to cancel the appointment -->
                    <form action="/user/dashboard/cancel-appointment/<%= appointment._id %>" method="POST">
                        <button type="submit">Cancel Appointment</button>
                    </form>
                </li>
            <% }) %>
        </ul>
    <% } else { %>
        <p class="no-appointments">You have no appointments scheduled.</p>
    <% } %>

    <a href="/user/dashboard">Back to Dashboard</a>
</body>
</html>
