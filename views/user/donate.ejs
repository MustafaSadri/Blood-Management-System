<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select a Blood Camp</title>
    <style>
        body {
            font-family: 'Open Sans', sans-serif;
            background-color: #F9F9F9;
            color: #333333;
            margin: 0;
            padding: 20px;
        }

        h1 {
            color: #C72C41;
            text-align: center;
            margin-bottom: 20px;
        }

        ul {
            list-style-type: none; /* Remove default list styling */
            padding: 0;
        }

        li {
            background-color: #ffffff;
            border: 1px solid #ccc;
            border-radius: 8px;
            padding: 15px;
            margin: 10px 0;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s;
        }

        li:hover {
            transform: scale(1.02); /* Slightly increase size on hover */
        }

        p {
            margin: 5px 0; /* Spacing between paragraphs */
        }

        button {
            background-color: #C72C41;
            color: #FFFFFF;
            border: none;
            padding: 10px 15px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #A82333; /* Darker shade on hover */
        }

        .no-camps {
            text-align: center;
            font-size: 18px;
            color: #666;
        }
    </style>
</head>
<body>
    <h1>Select a Blood Camp to Donate</h1>

    <% if (camps.length > 0) { %>
        <ul>
            <% camps.forEach(camp => { %>
                <li>
                    <h3><%= camp.name %></h3>
                    <p>Location: <%= camp.location %></p>
                    <p>Date: <%= new Date(camp.date).toLocaleDateString() %></p>
                    <p>Description: <%= camp.description || 'No description available' %></p>
                    <form action="/user/dashboard/donate/<%= camp._id %>" method="POST">
                        <button type="submit">Appoint this Camp</button>
                    </form>
                </li>
            <% }) %>
        </ul>
    <% } else { %>
        <p class="no-camps">No upcoming camps available.</p>
    <% } %>
</body>
</html>
